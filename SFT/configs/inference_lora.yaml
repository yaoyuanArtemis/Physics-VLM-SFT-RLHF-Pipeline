### model
# 基础模型路径（修改为你的实际路径）
model_name_or_path: models/Qwen2.5-VL-7B-Instruct

# SFT LoRA 权重路径（训练输出目录）
adapter_name_or_path: output/qwen_physics_v1

# 模板类型
template: qwen2_5_vl

### method
stage: sft                # SFT 推理阶段
finetuning_type: lora     # 使用 LoRA 适配器

### inference (可选配置)
# do_sample: true
# temperature: 0.7
# top_p: 0.9
# max_new_tokens: 512
